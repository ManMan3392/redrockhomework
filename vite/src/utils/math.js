export const add = (a, b) => {
  return a + b;
}

export const minus = (a, b) => {
  return a - b;
}

export const multi = (a, b) => {
  return a * b;
}

export const div = (a, b) => {
  return a / b;
}
